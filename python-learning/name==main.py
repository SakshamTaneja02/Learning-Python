import importing_function2 as if2
#after imoorting the module all the code in modules also gets executed which is a big problem to avoid this we use 
#name==main in the importing function

if2.welcome()