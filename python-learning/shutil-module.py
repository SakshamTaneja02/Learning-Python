import shutil

# shutil.copy("file.txt","file2.txt") copies text from file to file2
# shutil.copy2("file.txt","file2.txt") same af copy but preserves more metadata about the orginal file

# shutil.copytree(src,dest) copies the whole folder from src to dest

# shutil.move(src,dest) moves the file from src to dest

# shutil.rmtree(path) deletes a folder
